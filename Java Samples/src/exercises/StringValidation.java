package exercises;

public class StringValidation {
    public static void main(String[] args) {
        // Accept a String as an input
        // check the String to contain the following
        // 1. atleast 1 capital letter (A-Z)
        // 2. The length should be a minimum of 8 characters
        // 3. Contain atleast digit

        // if all the 3 validations are adhered to print "OK" else "Retry"
    }
}
